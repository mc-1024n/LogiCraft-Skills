## 의존 계층 (fan-out 라운드 순서)

도메인 간 계약 의존 때문에 완전 병렬이 불가하다. 아래 계층 순서로 라운드를 나눈다. **계층 안에서만 병렬**, 계층 간에는 앞 계층 QA pass 후 다음 계층.

```
{{layer_rounds}}
```

> - 계층 내용(어느 도메인이 몇 계층)은 이 프로젝트의 계약 의존을 근거로 온보딩이 확정한 값이다.
> - 프론트(있으면 최종 계층)는 소비할 백엔드 계약별로 점진 — 전 화면 일괄이 아니라 계약 준비된 화면부터.
